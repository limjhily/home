name: 공고문 PDF 구조 정찰

on:
  push:
    branches: [main]
    paths:
      - "scripts/probe_applyhome.py"
      - "scripts/probe_reby24.py"
  workflow_dispatch:
    inputs:
      house_nos:
        description: "확인할 공고번호 (공백으로 구분). 비워두면 기본값 사용"
        required: false
        default: ""

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install --quiet pdfplumber
      - name: 청약홈 정찰
        run: python3 scripts/probe_applyhome.py ${{ github.event.inputs.house_nos }}

      - name: 분양24 구조 조사
        if: always()
        run: python3 scripts/probe_reby24.py
      - name: 받은 PDF 보관
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: probe-pdf
          path: probe.pdf
          if-no-files-found: ignore
